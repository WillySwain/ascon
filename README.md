# Ascon128a implementation in Sage
To test, run `sage run_tests`
To add custom tests, add them to the test_parameters.txt file in the following format: 
`test_name,Key,Nonce,Associated Data,Plaintext`
